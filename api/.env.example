# Optional override; the default API port is 3001.
PORT=3001

# Server-side key with Routes API enabled and billing configured.
GOOGLE_MAPS_API_KEY=
# Fixed destination. Supply the actual company coordinates; no default is assumed.
COMPANY_LATITUDE=
COMPANY_LONGITUDE=
